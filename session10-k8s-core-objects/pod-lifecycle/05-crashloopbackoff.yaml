apiVersion: v1
kind: Pod
metadata:
  name: lifecycle-crashloop
spec:
  restartPolicy: Always
  containers:
    - name: crasher
      image: busybox:1.36
      command: ["sh", "-c", "echo 'starting...'; sleep 2; echo 'crashing now'; exit 1"]
