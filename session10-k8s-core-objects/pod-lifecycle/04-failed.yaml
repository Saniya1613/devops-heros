apiVersion: v1
kind: Pod
metadata:
  name: lifecycle-failed
spec:
  restartPolicy: Never
  containers:
    - name: batch
      image: busybox:1.36
      command: ["sh", "-c", "echo 'fatal error'; exit 1"]
