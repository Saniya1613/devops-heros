apiVersion: v1
kind: ConfigMap
metadata:
  name: myapp-html-blue
data:
  index.html: |
    <html><body><h1>myapp</h1><p>BLUE ENVIRONMENT - v1</p></body></html>
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-blue
spec:
  replicas: 3
  selector:
    matchLabels:
      app: myapp
      slot: blue
  template:
    metadata:
      labels:
        app: myapp
        slot: blue
        version: v1
    spec:
      containers:
        - name: web
          image: nginx:1.25-alpine
          ports:
            - containerPort: 80
          volumeMounts:
            - name: html
              mountPath: /usr/share/nginx/html
      volumes:
        - name: html
          configMap:
            name: myapp-html-blue
