apiVersion: v1
kind: Service
metadata:
  name: web-service-clusterip
spec:
  type: ClusterIP
  selector:
    app: web-clusterip
  ports:
    - protocol: TCP
      port: 8080        # Service VIP port
      targetPort: 80    # Pod containerPort
