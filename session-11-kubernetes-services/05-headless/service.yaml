apiVersion: v1
kind: Service
metadata:
  name: web-service-headless
spec:
  clusterIP: None        # <-- Headless: no virtual IP allocated
  selector:
    app: web-headless
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
